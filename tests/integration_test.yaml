#Name the configuration set, this can be used to track multiple configurations runs and changes
name: test
objects:
  notebook:
    # Notebook path can be a string, a list or a YAML items collection (multiple subgroups starting with - )
    notebook_path: "/"
    # Use Custom map var to setup a new location
    ## custom_map_vars:
    ##   path: "/Users/%{DATA:variable}/%{GREEDYDATA}"

  cluster_policy:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
  dbfs_file:
    # DBFS path can be a string, a list or a YAML items collection (multiple subgroups starting with - )
    dbfs_path:
      - "dbfs:/databricks/init_scripts"
      - [ "dbfs:/databricks/init/vj-H2O","dbfs:/databricks/init/vj-demo","dbfs:/databricks/init/vj-dl-py3","dbfs:/databricks/init/vj-gpu-demo" ]

  instance_pool:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
  secret:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
  cluster:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
  job:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
  identity:
    # pattern will be implemented in the future - make sure you have "*" in here
    patterns:
      - "*"
